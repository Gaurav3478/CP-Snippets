const ll MAXN = 1e7 + 5;
ll spf[MAXN];
 
void sieve() {
    for(ll i = 0; i < MAXN; i++) {
        spf[i] = i;
    }
    for(ll i = 2 ; i * i < MAXN; i++) {
        if(spf[i] == i) {
            for(ll j = i * i; j < MAXN ; j += i) {
                spf[j] = i;
            }
        }
    }   
} 
